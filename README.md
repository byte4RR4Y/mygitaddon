# mygitaddon
git cloner for kodi on linux

Install dependencies:
------------------------------------
     sudo apt update -y
     sudo apt install pip -y
     pip install GitPython
------------------------------------
