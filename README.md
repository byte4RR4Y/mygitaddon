# mygitaddon
git cloner for kodi on linux

Install dependencies:
------------------------------------
     sudo apt update -y
     sudo apt install pip
     pip install GitPython
------------------------------------
